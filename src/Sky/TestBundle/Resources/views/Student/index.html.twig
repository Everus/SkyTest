{% extends 'SkyTestBundle::layout.html.twig' %}

{% block body %}
    <div class="large-12 columns">
        <div class="row">

        </div>
        <div>
            {% for teacher in student.teachers.iterator %}
                {{ teacher.name }}
            {% else %}
                <p>У этого ученика нет учителей, хотите добавить?</p>
            {% endfor %}
        </div>
    </div>
{% endblock %}